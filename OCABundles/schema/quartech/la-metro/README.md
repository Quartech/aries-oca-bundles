# LA Volunteer mDoc – Minimal OCA Bundle
This directory contains the authoritative `OCABundle.json` for the LA Volunteer mDoc. The legacy `branding.json` file is retained only for simple card styling / backward compatibility and may not match the overlay format used elsewhere.

docType: com.lametro.volunteer
primary attribute → role
expiry attribute → expiry_date